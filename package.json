{
  "name": "deephaven-plugin-node-editor-e2e",
  "private": true,
  "description": "End to end tests for the node editor plugin",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
